/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getHubConfigDecoder,
  getHubConfigEncoder,
  type HubConfig,
  type HubConfigArgs,
} from "../types";

export const UPDATE_HUB_CONFIG_DISCRIMINATOR = new Uint8Array([
  73, 46, 233, 56, 245, 217, 38, 134,
]);

export function getUpdateHubConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_HUB_CONFIG_DISCRIMINATOR,
  );
}

export type UpdateHubConfigInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountHub extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHub extends string ? WritableAccount<TAccountHub> : TAccountHub,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateHubConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  config: HubConfig;
};

export type UpdateHubConfigInstructionDataArgs = { config: HubConfigArgs };

export function getUpdateHubConfigInstructionDataEncoder(): FixedSizeEncoder<UpdateHubConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["config", getHubConfigEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_HUB_CONFIG_DISCRIMINATOR }),
  );
}

export function getUpdateHubConfigInstructionDataDecoder(): FixedSizeDecoder<UpdateHubConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["config", getHubConfigDecoder()],
  ]);
}

export function getUpdateHubConfigInstructionDataCodec(): FixedSizeCodec<
  UpdateHubConfigInstructionDataArgs,
  UpdateHubConfigInstructionData
> {
  return combineCodec(
    getUpdateHubConfigInstructionDataEncoder(),
    getUpdateHubConfigInstructionDataDecoder(),
  );
}

export type UpdateHubConfigAsyncInput<
  TAccountHub extends string = string,
  TAccountAuthority extends string = string,
> = {
  hub?: Address<TAccountHub>;
  authority: TransactionSigner<TAccountAuthority>;
  config: UpdateHubConfigInstructionDataArgs["config"];
};

export async function getUpdateHubConfigInstructionAsync<
  TAccountHub extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: UpdateHubConfigAsyncInput<TAccountHub, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateHubConfigInstruction<TProgramAddress, TAccountHub, TAccountAuthority>
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hub: { value: input.hub ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hub.value) {
    accounts.hub.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([104, 117, 98]))],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.hub),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateHubConfigInstructionDataEncoder().encode(
      args as UpdateHubConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateHubConfigInstruction<
    TProgramAddress,
    TAccountHub,
    TAccountAuthority
  >);
}

export type UpdateHubConfigInput<
  TAccountHub extends string = string,
  TAccountAuthority extends string = string,
> = {
  hub: Address<TAccountHub>;
  authority: TransactionSigner<TAccountAuthority>;
  config: UpdateHubConfigInstructionDataArgs["config"];
};

export function getUpdateHubConfigInstruction<
  TAccountHub extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: UpdateHubConfigInput<TAccountHub, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateHubConfigInstruction<TProgramAddress, TAccountHub, TAccountAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hub: { value: input.hub ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.hub),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateHubConfigInstructionDataEncoder().encode(
      args as UpdateHubConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateHubConfigInstruction<
    TProgramAddress,
    TAccountHub,
    TAccountAuthority
  >);
}

export type ParsedUpdateHubConfigInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    hub: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateHubConfigInstructionData;
};

export function parseUpdateHubConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateHubConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { hub: getNextAccount(), authority: getNextAccount() },
    data: getUpdateHubConfigInstructionDataDecoder().decode(instruction.data),
  };
}

/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ENDORSE_CREDENTIAL_DISCRIMINATOR = new Uint8Array([
  89, 7, 229, 22, 151, 137, 59, 148,
]);

export function getEndorseCredentialDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ENDORSE_CREDENTIAL_DISCRIMINATOR,
  );
}

export type EndorseCredentialInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountCredential extends string | AccountMeta<string> = string,
  TAccountMentor extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCredential extends string
        ? WritableAccount<TAccountCredential>
        : TAccountCredential,
      TAccountMentor extends string
        ? WritableSignerAccount<TAccountMentor> &
            AccountSignerMeta<TAccountMentor>
        : TAccountMentor,
      ...TRemainingAccounts,
    ]
  >;

export type EndorseCredentialInstructionData = {
  discriminator: ReadonlyUint8Array;
  endorsementMessage: string;
};

export type EndorseCredentialInstructionDataArgs = {
  endorsementMessage: string;
};

export function getEndorseCredentialInstructionDataEncoder(): Encoder<EndorseCredentialInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "endorsementMessage",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: ENDORSE_CREDENTIAL_DISCRIMINATOR }),
  );
}

export function getEndorseCredentialInstructionDataDecoder(): Decoder<EndorseCredentialInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "endorsementMessage",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
  ]);
}

export function getEndorseCredentialInstructionDataCodec(): Codec<
  EndorseCredentialInstructionDataArgs,
  EndorseCredentialInstructionData
> {
  return combineCodec(
    getEndorseCredentialInstructionDataEncoder(),
    getEndorseCredentialInstructionDataDecoder(),
  );
}

export type EndorseCredentialInput<
  TAccountCredential extends string = string,
  TAccountMentor extends string = string,
> = {
  credential: Address<TAccountCredential>;
  mentor: TransactionSigner<TAccountMentor>;
  endorsementMessage: EndorseCredentialInstructionDataArgs["endorsementMessage"];
};

export function getEndorseCredentialInstruction<
  TAccountCredential extends string,
  TAccountMentor extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: EndorseCredentialInput<TAccountCredential, TAccountMentor>,
  config?: { programAddress?: TProgramAddress },
): EndorseCredentialInstruction<
  TProgramAddress,
  TAccountCredential,
  TAccountMentor
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    credential: { value: input.credential ?? null, isWritable: true },
    mentor: { value: input.mentor ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.credential),
      getAccountMeta(accounts.mentor),
    ],
    data: getEndorseCredentialInstructionDataEncoder().encode(
      args as EndorseCredentialInstructionDataArgs,
    ),
    programAddress,
  } as EndorseCredentialInstruction<
    TProgramAddress,
    TAccountCredential,
    TAccountMentor
  >);
}

export type ParsedEndorseCredentialInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    credential: TAccountMetas[0];
    mentor: TAccountMetas[1];
  };
  data: EndorseCredentialInstructionData;
};

export function parseEndorseCredentialInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEndorseCredentialInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { credential: getNextAccount(), mentor: getNextAccount() },
    data: getEndorseCredentialInstructionDataDecoder().decode(instruction.data),
  };
}

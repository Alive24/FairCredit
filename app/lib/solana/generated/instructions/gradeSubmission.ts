/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getF64Decoder,
  getF64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GRADE_SUBMISSION_DISCRIMINATOR = new Uint8Array([
  184, 10, 237, 142, 22, 63, 207, 33,
]);

export function getGradeSubmissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GRADE_SUBMISSION_DISCRIMINATOR,
  );
}

export type GradeSubmissionInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountSubmission extends string | AccountMeta<string> = string,
  TAccountGrader extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSubmission extends string
        ? WritableAccount<TAccountSubmission>
        : TAccountSubmission,
      TAccountGrader extends string
        ? WritableSignerAccount<TAccountGrader> &
            AccountSignerMeta<TAccountGrader>
        : TAccountGrader,
      ...TRemainingAccounts,
    ]
  >;

export type GradeSubmissionInstructionData = {
  discriminator: ReadonlyUint8Array;
  grade: number;
  feedback: Option<string>;
};

export type GradeSubmissionInstructionDataArgs = {
  grade: number;
  feedback: OptionOrNullable<string>;
};

export function getGradeSubmissionInstructionDataEncoder(): Encoder<GradeSubmissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["grade", getF64Encoder()],
      [
        "feedback",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: GRADE_SUBMISSION_DISCRIMINATOR }),
  );
}

export function getGradeSubmissionInstructionDataDecoder(): Decoder<GradeSubmissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["grade", getF64Decoder()],
    [
      "feedback",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getGradeSubmissionInstructionDataCodec(): Codec<
  GradeSubmissionInstructionDataArgs,
  GradeSubmissionInstructionData
> {
  return combineCodec(
    getGradeSubmissionInstructionDataEncoder(),
    getGradeSubmissionInstructionDataDecoder(),
  );
}

export type GradeSubmissionInput<
  TAccountSubmission extends string = string,
  TAccountGrader extends string = string,
> = {
  submission: Address<TAccountSubmission>;
  grader: TransactionSigner<TAccountGrader>;
  grade: GradeSubmissionInstructionDataArgs["grade"];
  feedback: GradeSubmissionInstructionDataArgs["feedback"];
};

export function getGradeSubmissionInstruction<
  TAccountSubmission extends string,
  TAccountGrader extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: GradeSubmissionInput<TAccountSubmission, TAccountGrader>,
  config?: { programAddress?: TProgramAddress },
): GradeSubmissionInstruction<
  TProgramAddress,
  TAccountSubmission,
  TAccountGrader
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    submission: { value: input.submission ?? null, isWritable: true },
    grader: { value: input.grader ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.submission),
      getAccountMeta(accounts.grader),
    ],
    data: getGradeSubmissionInstructionDataEncoder().encode(
      args as GradeSubmissionInstructionDataArgs,
    ),
    programAddress,
  } as GradeSubmissionInstruction<
    TProgramAddress,
    TAccountSubmission,
    TAccountGrader
  >);
}

export type ParsedGradeSubmissionInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    submission: TAccountMetas[0];
    grader: TAccountMetas[1];
  };
  data: GradeSubmissionInstructionData;
};

export function parseGradeSubmissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGradeSubmissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { submission: getNextAccount(), grader: getNextAccount() },
    data: getGradeSubmissionInstructionDataDecoder().decode(instruction.data),
  };
}

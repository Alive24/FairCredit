/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_VERIFIER_DISCRIMINATOR = new Uint8Array([
  94, 212, 44, 130, 71, 242, 204, 155,
]);

export function getInitializeVerifierDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_VERIFIER_DISCRIMINATOR,
  );
}

export type InitializeVerifierInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountVerifierAccount extends string | AccountMeta<string> = string,
  TAccountVerifierAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVerifierAccount extends string
        ? WritableAccount<TAccountVerifierAccount>
        : TAccountVerifierAccount,
      TAccountVerifierAuthority extends string
        ? WritableSignerAccount<TAccountVerifierAuthority> &
            AccountSignerMeta<TAccountVerifierAuthority>
        : TAccountVerifierAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeVerifierInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeVerifierInstructionDataArgs = {};

export function getInitializeVerifierInstructionDataEncoder(): FixedSizeEncoder<InitializeVerifierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIALIZE_VERIFIER_DISCRIMINATOR }),
  );
}

export function getInitializeVerifierInstructionDataDecoder(): FixedSizeDecoder<InitializeVerifierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeVerifierInstructionDataCodec(): FixedSizeCodec<
  InitializeVerifierInstructionDataArgs,
  InitializeVerifierInstructionData
> {
  return combineCodec(
    getInitializeVerifierInstructionDataEncoder(),
    getInitializeVerifierInstructionDataDecoder(),
  );
}

export type InitializeVerifierAsyncInput<
  TAccountVerifierAccount extends string = string,
  TAccountVerifierAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  verifierAccount?: Address<TAccountVerifierAccount>;
  verifierAuthority: TransactionSigner<TAccountVerifierAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeVerifierInstructionAsync<
  TAccountVerifierAccount extends string,
  TAccountVerifierAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: InitializeVerifierAsyncInput<
    TAccountVerifierAccount,
    TAccountVerifierAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeVerifierInstruction<
    TProgramAddress,
    TAccountVerifierAccount,
    TAccountVerifierAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    verifierAccount: { value: input.verifierAccount ?? null, isWritable: true },
    verifierAuthority: {
      value: input.verifierAuthority ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.verifierAccount.value) {
    accounts.verifierAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([118, 101, 114, 105, 102, 105, 101, 114]),
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.verifierAuthority.value),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.verifierAccount),
      getAccountMeta(accounts.verifierAuthority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeVerifierInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeVerifierInstruction<
    TProgramAddress,
    TAccountVerifierAccount,
    TAccountVerifierAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeVerifierInput<
  TAccountVerifierAccount extends string = string,
  TAccountVerifierAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  verifierAccount: Address<TAccountVerifierAccount>;
  verifierAuthority: TransactionSigner<TAccountVerifierAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeVerifierInstruction<
  TAccountVerifierAccount extends string,
  TAccountVerifierAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: InitializeVerifierInput<
    TAccountVerifierAccount,
    TAccountVerifierAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeVerifierInstruction<
  TProgramAddress,
  TAccountVerifierAccount,
  TAccountVerifierAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    verifierAccount: { value: input.verifierAccount ?? null, isWritable: true },
    verifierAuthority: {
      value: input.verifierAuthority ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.verifierAccount),
      getAccountMeta(accounts.verifierAuthority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeVerifierInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeVerifierInstruction<
    TProgramAddress,
    TAccountVerifierAccount,
    TAccountVerifierAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeVerifierInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    verifierAccount: TAccountMetas[0];
    verifierAuthority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeVerifierInstructionData;
};

export function parseInitializeVerifierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeVerifierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      verifierAccount: getNextAccount(),
      verifierAuthority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeVerifierInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

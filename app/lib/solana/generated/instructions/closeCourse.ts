/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_COURSE_DISCRIMINATOR = new Uint8Array([
  157, 252, 239, 166, 213, 174, 160, 34,
]);

export function getCloseCourseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_COURSE_DISCRIMINATOR,
  );
}

export type CloseCourseInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountCourse extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountHub extends string | AccountMeta<string> = string,
  TAccountProviderAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCourse extends string
        ? WritableAccount<TAccountCourse>
        : TAccountCourse,
      TAccountProvider extends string
        ? WritableAccount<TAccountProvider>
        : TAccountProvider,
      TAccountHub extends string ? WritableAccount<TAccountHub> : TAccountHub,
      TAccountProviderAuthority extends string
        ? WritableSignerAccount<TAccountProviderAuthority> &
            AccountSignerMeta<TAccountProviderAuthority>
        : TAccountProviderAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CloseCourseInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseCourseInstructionDataArgs = {};

export function getCloseCourseInstructionDataEncoder(): FixedSizeEncoder<CloseCourseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_COURSE_DISCRIMINATOR }),
  );
}

export function getCloseCourseInstructionDataDecoder(): FixedSizeDecoder<CloseCourseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseCourseInstructionDataCodec(): FixedSizeCodec<
  CloseCourseInstructionDataArgs,
  CloseCourseInstructionData
> {
  return combineCodec(
    getCloseCourseInstructionDataEncoder(),
    getCloseCourseInstructionDataDecoder(),
  );
}

export type CloseCourseAsyncInput<
  TAccountCourse extends string = string,
  TAccountProvider extends string = string,
  TAccountHub extends string = string,
  TAccountProviderAuthority extends string = string,
> = {
  course: Address<TAccountCourse>;
  provider?: Address<TAccountProvider>;
  hub?: Address<TAccountHub>;
  providerAuthority: TransactionSigner<TAccountProviderAuthority>;
};

export async function getCloseCourseInstructionAsync<
  TAccountCourse extends string,
  TAccountProvider extends string,
  TAccountHub extends string,
  TAccountProviderAuthority extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: CloseCourseAsyncInput<
    TAccountCourse,
    TAccountProvider,
    TAccountHub,
    TAccountProviderAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseCourseInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountProvider,
    TAccountHub,
    TAccountProviderAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    course: { value: input.course ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: true },
    hub: { value: input.hub ?? null, isWritable: true },
    providerAuthority: {
      value: input.providerAuthority ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.hub.value) {
    accounts.hub.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([104, 117, 98]))],
    });
  }
  if (!accounts.provider.value) {
    accounts.provider.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 114, 111, 118, 105, 100, 101, 114]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.hub.value)),
        getAddressEncoder().encode(
          expectAddress(accounts.providerAuthority.value),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.course),
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.hub),
      getAccountMeta(accounts.providerAuthority),
    ],
    data: getCloseCourseInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseCourseInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountProvider,
    TAccountHub,
    TAccountProviderAuthority
  >);
}

export type CloseCourseInput<
  TAccountCourse extends string = string,
  TAccountProvider extends string = string,
  TAccountHub extends string = string,
  TAccountProviderAuthority extends string = string,
> = {
  course: Address<TAccountCourse>;
  provider: Address<TAccountProvider>;
  hub: Address<TAccountHub>;
  providerAuthority: TransactionSigner<TAccountProviderAuthority>;
};

export function getCloseCourseInstruction<
  TAccountCourse extends string,
  TAccountProvider extends string,
  TAccountHub extends string,
  TAccountProviderAuthority extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: CloseCourseInput<
    TAccountCourse,
    TAccountProvider,
    TAccountHub,
    TAccountProviderAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): CloseCourseInstruction<
  TProgramAddress,
  TAccountCourse,
  TAccountProvider,
  TAccountHub,
  TAccountProviderAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    course: { value: input.course ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: true },
    hub: { value: input.hub ?? null, isWritable: true },
    providerAuthority: {
      value: input.providerAuthority ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.course),
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.hub),
      getAccountMeta(accounts.providerAuthority),
    ],
    data: getCloseCourseInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseCourseInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountProvider,
    TAccountHub,
    TAccountProviderAuthority
  >);
}

export type ParsedCloseCourseInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    course: TAccountMetas[0];
    provider: TAccountMetas[1];
    hub: TAccountMetas[2];
    providerAuthority: TAccountMetas[3];
  };
  data: CloseCourseInstructionData;
};

export function parseCloseCourseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseCourseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      course: getNextAccount(),
      provider: getNextAccount(),
      hub: getNextAccount(),
      providerAuthority: getNextAccount(),
    },
    data: getCloseCourseInstructionDataDecoder().decode(instruction.data),
  };
}

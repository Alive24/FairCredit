/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ADD_ATTENDANCE_DISCRIMINATOR = new Uint8Array([
  14, 218, 169, 73, 17, 67, 117, 232,
]);

export function getAddAttendanceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADD_ATTENDANCE_DISCRIMINATOR,
  );
}

export type AddAttendanceInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountActivity extends string | AccountMeta<string> = string,
  TAccountStudentAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountActivity extends string
        ? WritableAccount<TAccountActivity>
        : TAccountActivity,
      TAccountStudentAuthority extends string
        ? WritableSignerAccount<TAccountStudentAuthority> &
            AccountSignerMeta<TAccountStudentAuthority>
        : TAccountStudentAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type AddAttendanceInstructionData = {
  discriminator: ReadonlyUint8Array;
  timestamp: Option<string>;
};

export type AddAttendanceInstructionDataArgs = {
  timestamp: OptionOrNullable<string>;
};

export function getAddAttendanceInstructionDataEncoder(): Encoder<AddAttendanceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "timestamp",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: ADD_ATTENDANCE_DISCRIMINATOR }),
  );
}

export function getAddAttendanceInstructionDataDecoder(): Decoder<AddAttendanceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "timestamp",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getAddAttendanceInstructionDataCodec(): Codec<
  AddAttendanceInstructionDataArgs,
  AddAttendanceInstructionData
> {
  return combineCodec(
    getAddAttendanceInstructionDataEncoder(),
    getAddAttendanceInstructionDataDecoder(),
  );
}

export type AddAttendanceInput<
  TAccountActivity extends string = string,
  TAccountStudentAuthority extends string = string,
> = {
  activity: Address<TAccountActivity>;
  studentAuthority: TransactionSigner<TAccountStudentAuthority>;
  timestamp: AddAttendanceInstructionDataArgs["timestamp"];
};

export function getAddAttendanceInstruction<
  TAccountActivity extends string,
  TAccountStudentAuthority extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: AddAttendanceInput<TAccountActivity, TAccountStudentAuthority>,
  config?: { programAddress?: TProgramAddress },
): AddAttendanceInstruction<
  TProgramAddress,
  TAccountActivity,
  TAccountStudentAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    activity: { value: input.activity ?? null, isWritable: true },
    studentAuthority: {
      value: input.studentAuthority ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.activity),
      getAccountMeta(accounts.studentAuthority),
    ],
    data: getAddAttendanceInstructionDataEncoder().encode(
      args as AddAttendanceInstructionDataArgs,
    ),
    programAddress,
  } as AddAttendanceInstruction<
    TProgramAddress,
    TAccountActivity,
    TAccountStudentAuthority
  >);
}

export type ParsedAddAttendanceInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    activity: TAccountMetas[0];
    studentAuthority: TAccountMetas[1];
  };
  data: AddAttendanceInstructionData;
};

export function parseAddAttendanceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddAttendanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      activity: getNextAccount(),
      studentAuthority: getNextAccount(),
    },
    data: getAddAttendanceInstructionDataDecoder().decode(instruction.data),
  };
}

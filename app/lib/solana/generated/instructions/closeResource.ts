/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { FAIR_CREDIT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLOSE_RESOURCE_DISCRIMINATOR = new Uint8Array([
  124, 213, 147, 66, 133, 230, 67, 224,
]);

export function getCloseResourceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_RESOURCE_DISCRIMINATOR,
  );
}

export type CloseResourceInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountResource extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountResource extends string
        ? WritableAccount<TAccountResource>
        : TAccountResource,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CloseResourceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseResourceInstructionDataArgs = {};

export function getCloseResourceInstructionDataEncoder(): FixedSizeEncoder<CloseResourceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_RESOURCE_DISCRIMINATOR }),
  );
}

export function getCloseResourceInstructionDataDecoder(): FixedSizeDecoder<CloseResourceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseResourceInstructionDataCodec(): FixedSizeCodec<
  CloseResourceInstructionDataArgs,
  CloseResourceInstructionData
> {
  return combineCodec(
    getCloseResourceInstructionDataEncoder(),
    getCloseResourceInstructionDataDecoder(),
  );
}

export type CloseResourceInput<
  TAccountResource extends string = string,
  TAccountAuthority extends string = string,
> = {
  resource: Address<TAccountResource>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getCloseResourceInstruction<
  TAccountResource extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
>(
  input: CloseResourceInput<TAccountResource, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): CloseResourceInstruction<
  TProgramAddress,
  TAccountResource,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAIR_CREDIT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    resource: { value: input.resource ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.resource),
      getAccountMeta(accounts.authority),
    ],
    data: getCloseResourceInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseResourceInstruction<
    TProgramAddress,
    TAccountResource,
    TAccountAuthority
  >);
}

export type ParsedCloseResourceInstruction<
  TProgram extends string = typeof FAIR_CREDIT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    resource: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: CloseResourceInstructionData;
};

export function parseCloseResourceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseResourceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { resource: getNextAccount(), authority: getNextAccount() },
    data: getCloseResourceInstructionDataDecoder().decode(instruction.data),
  };
}

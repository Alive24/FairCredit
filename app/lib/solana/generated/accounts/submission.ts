/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getF64Decoder,
  getF64Encoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getSubmissionStatusDecoder,
  getSubmissionStatusEncoder,
  type SubmissionStatus,
  type SubmissionStatusArgs,
} from "../types";

export const SUBMISSION_DISCRIMINATOR = new Uint8Array([
  58, 194, 159, 158, 75, 102, 178, 197,
]);

export function getSubmissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SUBMISSION_DISCRIMINATOR);
}

export type Submission = {
  discriminator: ReadonlyUint8Array;
  /** Submission timestamp (also part of PDA seeds) */
  submittedAt: bigint;
  /** Last update timestamp */
  updated: bigint;
  /** Resource this submission is for */
  resource: Address;
  /** Student wallet */
  student: Address;
  /** Attached asset PDAs */
  assets: Array<Address>;
  /** Evidence asset PDAs */
  evidenceAssets: Array<Address>;
  status: SubmissionStatus;
  grade: Option<number>;
  feedback: Option<string>;
  gradedBy: Option<Address>;
  gradedAt: Option<bigint>;
  /** Optional Nostr "d" tag used to locate the latest editable content event */
  nostrDTag: Option<string>;
  /** Nostr author public key bytes (32 bytes). All-zero means "not set". */
  nostrAuthorPubkey: ReadonlyUint8Array;
  /** Optional Walrus blob identifier for the submission content bundle */
  walrusBlobId: Option<string>;
};

export type SubmissionArgs = {
  /** Submission timestamp (also part of PDA seeds) */
  submittedAt: number | bigint;
  /** Last update timestamp */
  updated: number | bigint;
  /** Resource this submission is for */
  resource: Address;
  /** Student wallet */
  student: Address;
  /** Attached asset PDAs */
  assets: Array<Address>;
  /** Evidence asset PDAs */
  evidenceAssets: Array<Address>;
  status: SubmissionStatusArgs;
  grade: OptionOrNullable<number>;
  feedback: OptionOrNullable<string>;
  gradedBy: OptionOrNullable<Address>;
  gradedAt: OptionOrNullable<number | bigint>;
  /** Optional Nostr "d" tag used to locate the latest editable content event */
  nostrDTag: OptionOrNullable<string>;
  /** Nostr author public key bytes (32 bytes). All-zero means "not set". */
  nostrAuthorPubkey: ReadonlyUint8Array;
  /** Optional Walrus blob identifier for the submission content bundle */
  walrusBlobId: OptionOrNullable<string>;
};

/** Gets the encoder for {@link SubmissionArgs} account data. */
export function getSubmissionEncoder(): Encoder<SubmissionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["submittedAt", getI64Encoder()],
      ["updated", getI64Encoder()],
      ["resource", getAddressEncoder()],
      ["student", getAddressEncoder()],
      ["assets", getArrayEncoder(getAddressEncoder())],
      ["evidenceAssets", getArrayEncoder(getAddressEncoder())],
      ["status", getSubmissionStatusEncoder()],
      ["grade", getOptionEncoder(getF64Encoder())],
      [
        "feedback",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["gradedBy", getOptionEncoder(getAddressEncoder())],
      ["gradedAt", getOptionEncoder(getI64Encoder())],
      [
        "nostrDTag",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["nostrAuthorPubkey", fixEncoderSize(getBytesEncoder(), 32)],
      [
        "walrusBlobId",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: SUBMISSION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Submission} account data. */
export function getSubmissionDecoder(): Decoder<Submission> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["submittedAt", getI64Decoder()],
    ["updated", getI64Decoder()],
    ["resource", getAddressDecoder()],
    ["student", getAddressDecoder()],
    ["assets", getArrayDecoder(getAddressDecoder())],
    ["evidenceAssets", getArrayDecoder(getAddressDecoder())],
    ["status", getSubmissionStatusDecoder()],
    ["grade", getOptionDecoder(getF64Decoder())],
    [
      "feedback",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["gradedBy", getOptionDecoder(getAddressDecoder())],
    ["gradedAt", getOptionDecoder(getI64Decoder())],
    [
      "nostrDTag",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["nostrAuthorPubkey", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "walrusBlobId",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

/** Gets the codec for {@link Submission} account data. */
export function getSubmissionCodec(): Codec<SubmissionArgs, Submission> {
  return combineCodec(getSubmissionEncoder(), getSubmissionDecoder());
}

export function decodeSubmission<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Submission, TAddress>;
export function decodeSubmission<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Submission, TAddress>;
export function decodeSubmission<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Submission, TAddress> | MaybeAccount<Submission, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSubmissionDecoder(),
  );
}

export async function fetchSubmission<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Submission, TAddress>> {
  const maybeAccount = await fetchMaybeSubmission(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSubmission<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Submission, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSubmission(maybeAccount);
}

export async function fetchAllSubmission(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Submission>[]> {
  const maybeAccounts = await fetchAllMaybeSubmission(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSubmission(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Submission>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSubmission(maybeAccount));
}

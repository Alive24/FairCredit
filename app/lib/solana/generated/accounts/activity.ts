/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getF64Decoder,
  getF64Encoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getActivityKindDecoder,
  getActivityKindEncoder,
  getActivityStatusDecoder,
  getActivityStatusEncoder,
  getResourceKindDecoder,
  getResourceKindEncoder,
  type ActivityKind,
  type ActivityKindArgs,
  type ActivityStatus,
  type ActivityStatusArgs,
  type ResourceKind,
  type ResourceKindArgs,
} from "../types";

export const ACTIVITY_DISCRIMINATOR = new Uint8Array([
  159, 236, 145, 113, 221, 192, 137, 112,
]);

export function getActivityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ACTIVITY_DISCRIMINATOR);
}

export type Activity = {
  discriminator: ReadonlyUint8Array;
  /** Creation timestamp (used in PDA seeds: activity + provider + student + created) */
  created: bigint;
  updated: bigint;
  /** Uploader / student wallet */
  student: Address;
  /** Provider account (PDA) */
  provider: Address;
  degreeId: Option<string>;
  /** Course account (PDA) if activity is course-related */
  course: Option<Address>;
  resourceId: Option<string>;
  data: string;
  kind: ActivityKind;
  status: ActivityStatus;
  resourceKind: Option<ResourceKind>;
  grade: Option<number>;
  assets: Array<string>;
  evidenceAssets: Array<string>;
};

export type ActivityArgs = {
  /** Creation timestamp (used in PDA seeds: activity + provider + student + created) */
  created: number | bigint;
  updated: number | bigint;
  /** Uploader / student wallet */
  student: Address;
  /** Provider account (PDA) */
  provider: Address;
  degreeId: OptionOrNullable<string>;
  /** Course account (PDA) if activity is course-related */
  course: OptionOrNullable<Address>;
  resourceId: OptionOrNullable<string>;
  data: string;
  kind: ActivityKindArgs;
  status: ActivityStatusArgs;
  resourceKind: OptionOrNullable<ResourceKindArgs>;
  grade: OptionOrNullable<number>;
  assets: Array<string>;
  evidenceAssets: Array<string>;
};

/** Gets the encoder for {@link ActivityArgs} account data. */
export function getActivityEncoder(): Encoder<ActivityArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["created", getI64Encoder()],
      ["updated", getI64Encoder()],
      ["student", getAddressEncoder()],
      ["provider", getAddressEncoder()],
      [
        "degreeId",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["course", getOptionEncoder(getAddressEncoder())],
      [
        "resourceId",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["data", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["kind", getActivityKindEncoder()],
      ["status", getActivityStatusEncoder()],
      ["resourceKind", getOptionEncoder(getResourceKindEncoder())],
      ["grade", getOptionEncoder(getF64Encoder())],
      [
        "assets",
        getArrayEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      [
        "evidenceAssets",
        getArrayEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: ACTIVITY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Activity} account data. */
export function getActivityDecoder(): Decoder<Activity> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["created", getI64Decoder()],
    ["updated", getI64Decoder()],
    ["student", getAddressDecoder()],
    ["provider", getAddressDecoder()],
    [
      "degreeId",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["course", getOptionDecoder(getAddressDecoder())],
    [
      "resourceId",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["data", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["kind", getActivityKindDecoder()],
    ["status", getActivityStatusDecoder()],
    ["resourceKind", getOptionDecoder(getResourceKindDecoder())],
    ["grade", getOptionDecoder(getF64Decoder())],
    [
      "assets",
      getArrayDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    [
      "evidenceAssets",
      getArrayDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

/** Gets the codec for {@link Activity} account data. */
export function getActivityCodec(): Codec<ActivityArgs, Activity> {
  return combineCodec(getActivityEncoder(), getActivityDecoder());
}

export function decodeActivity<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Activity, TAddress>;
export function decodeActivity<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Activity, TAddress>;
export function decodeActivity<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Activity, TAddress> | MaybeAccount<Activity, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getActivityDecoder(),
  );
}

export async function fetchActivity<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Activity, TAddress>> {
  const maybeAccount = await fetchMaybeActivity(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeActivity<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Activity, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeActivity(maybeAccount);
}

export async function fetchAllActivity(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Activity>[]> {
  const maybeAccounts = await fetchAllMaybeActivity(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeActivity(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Activity>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeActivity(maybeAccount));
}

/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getHubConfigDecoder,
  getHubConfigEncoder,
  type HubConfig,
  type HubConfigArgs,
} from "../types";

export const HUB_DISCRIMINATOR = new Uint8Array([
  143, 189, 4, 120, 169, 234, 14, 125,
]);

export function getHubDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HUB_DISCRIMINATOR);
}

export type Hub = {
  discriminator: ReadonlyUint8Array;
  /** Hub authority (can be multisig or DAO in future) */
  authority: Address;
  /** List of accepted provider wallets */
  acceptedProviders: Array<Address>;
  /**
   * List of accepted course PDAs (courses from accepted providers; must be accepted to be usable)
   * Entries can be direct course PDAs or CourseList PDAs for sharded storage.
   */
  acceptedCourses: Array<Address>;
  /** Hub creation timestamp */
  createdAt: bigint;
  /** Last update timestamp */
  updatedAt: bigint;
  /** Hub configuration flags */
  config: HubConfig;
};

export type HubArgs = {
  /** Hub authority (can be multisig or DAO in future) */
  authority: Address;
  /** List of accepted provider wallets */
  acceptedProviders: Array<Address>;
  /**
   * List of accepted course PDAs (courses from accepted providers; must be accepted to be usable)
   * Entries can be direct course PDAs or CourseList PDAs for sharded storage.
   */
  acceptedCourses: Array<Address>;
  /** Hub creation timestamp */
  createdAt: number | bigint;
  /** Last update timestamp */
  updatedAt: number | bigint;
  /** Hub configuration flags */
  config: HubConfigArgs;
};

/** Gets the encoder for {@link HubArgs} account data. */
export function getHubEncoder(): Encoder<HubArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["acceptedProviders", getArrayEncoder(getAddressEncoder())],
      ["acceptedCourses", getArrayEncoder(getAddressEncoder())],
      ["createdAt", getI64Encoder()],
      ["updatedAt", getI64Encoder()],
      ["config", getHubConfigEncoder()],
    ]),
    (value) => ({ ...value, discriminator: HUB_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Hub} account data. */
export function getHubDecoder(): Decoder<Hub> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["acceptedProviders", getArrayDecoder(getAddressDecoder())],
    ["acceptedCourses", getArrayDecoder(getAddressDecoder())],
    ["createdAt", getI64Decoder()],
    ["updatedAt", getI64Decoder()],
    ["config", getHubConfigDecoder()],
  ]);
}

/** Gets the codec for {@link Hub} account data. */
export function getHubCodec(): Codec<HubArgs, Hub> {
  return combineCodec(getHubEncoder(), getHubDecoder());
}

export function decodeHub<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Hub, TAddress>;
export function decodeHub<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Hub, TAddress>;
export function decodeHub<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Hub, TAddress> | MaybeAccount<Hub, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHubDecoder(),
  );
}

export async function fetchHub<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Hub, TAddress>> {
  const maybeAccount = await fetchMaybeHub(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHub<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Hub, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHub(maybeAccount);
}

export async function fetchAllHub(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Hub>[]> {
  const maybeAccounts = await fetchAllMaybeHub(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHub(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Hub>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeHub(maybeAccount));
}
